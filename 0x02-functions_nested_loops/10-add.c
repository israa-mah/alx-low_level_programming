#include "main.h"
/**
 * add - adds two integers and returns the result.
 * @a: the first number.
 * @b:the second number.
 * Return: addition result.
 */
int add(int a, int b)
{
	int c;

	c = a + b;
	return (c);
}
